    Python-приложение с Gunicorn:
        Представляет собой веб-приложение, написанное на Python, которое работает с использованием фреймворка Flask.
        Gunicorn выступает в качестве HTTP-сервера для обработки запросов к приложению.
        Приложение имеет несколько маршрутов (/start, /middle, ..., /end, /error), которые возвращают различные HTTP-коды состояния (201, 202, ..., 205, 500 соответственно).

    Logstash:
        Cистема сбора и обработки данных, которая отправляет их в различные хранилища, в том числе в Elasticsearch.
        Используется для сбора и преобразования логов перед отправкой их в Elasticsearch для последующего анализа и визуализации.
        Конфигурация Logstash определяет источники, фильтры и маршрутизацию логов к целевому хранилищу Elasticsearch.

    Elasticsearch:
        Elasticsearch - масштабируемая система полнотекстового поиска и аналитики, которая позволяет быстро и в режиме реального времени хранить, искать и анализировать большие объемы данных.
        Elasticsearch используется для хранения логов, собранных Logstash, и предоставляет интерфейс для их поиска и анализа.

    Grafana:
        Представляет собой веб-интерфейс для визуализации и анализа данных, хранящихся в Elasticsearch.
        Используется для создания интерактивных графиков, дашбордов и выполнения поисковых запросов по логам, что упрощает мониторинг и анализ данных.

Эти компоненты взаимодействуют следующим образом:

    Python-приложение генерирует логи в ответ на запросы клиентов.
    Logstash собирает эти логи, преобразует их при необходимости и отправляет в Elasticsearch для индексации.
    Elasticsearch хранит логи и предоставляет интерфейс для их поиска и анализа.
    Grafana используется для визуализации и анализа данных, хранящихся в Elasticsearch, позволяя пользователям создавать интерактивные графики, дашборды и выполнять запросы для исследования данных.
